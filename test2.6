#include <iostream>
#include<algorithm>
#include<stdio.h>
#include<string.h>
#include<vector>
using namespace std;



int main()
{
    int n;
    int k;
    while(scanf("%d",&n)==1&&scanf("%d",&k)==1)
    {
        vector<int> vec;
       int a[500];
       int b[500];
       for(int i=0;i<n;i++)
       {
       scanf("%d",&a[i]);
       vec.push_back(a[i]);
       }
       make_heap(vec.begin(),vec.end());
       for(int i=0;i<k;i++)
       {
                  b[i]=vec[0];
//       pop_heap(vec.begin(),vec.end());
       vec.erase(vec.begin());
       make_heap(vec.begin(),vec.end());
       }
       for(int i=0;i<k;i++)
       {
            printf("%d ", b[i]);
       }
    }
    return 0;
}
